-- how to insert, update, delete data

-- varchar vs char
-- char(50) -> if data is 5 chars then mysql will fill 45 extra characters 
-- varchar(50) -> limit is 50, but if hte data is 5 chars then mysql will NOT fill the missing 45
-- just click the wretch next to a table to see more info.