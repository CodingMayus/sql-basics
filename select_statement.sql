-- USE sql_store;

-- SELECT customer_id,first_name
-- FROM customers;
-- SELECT * 
-- FROM customers;
-- use WHERE clause to filter data

SELECT * 
FROM customers
-- WHERE customer_id IN (1,2,3)
ORDER BY first_name;
-- use ORDERBY to sort the data.

SELECT 1,2 FROM customers
-- order matters ( for syntax ) 